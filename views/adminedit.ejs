<!--DONE & COPIED OVER-->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Admin</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="plugins/bootstrap/bootstrap.min.css">

    <!-- Inline CSS -->
    <style>
        /* Turtle-specific styles */
        .turtle-container {
            margin-top: 50px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #f8f8f8;
        }

        .turtle-table-wrapper {
            background: #ffffff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.1);
            width: 90%;
            max-width: 600px;
        }

        .turtle-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 16px;
            text-align: left;
        }

        .turtle-table th {
            text-align: left;
            font-weight: bold;
            padding: 12px;
            background-color: #f8f9fa;
            color: #333;
            border: 1px solid #ddd;
        }

        .turtle-table td {
            padding: 12px;
            border: 1px solid #ddd;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-shadow: none;
        }

        .form-control:focus {
            border-color: #28ABE3;
            box-shadow: 0 0 5px rgba(40, 171, 227, 0.5);
        }

        .turtle-btn {
            padding: 10px 20px;
            background-color: #28ABE3;
            border: none;
            color: white;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .turtle-btn:hover {
            background-color: #1a86b0;
            transform: translateY(-2px);
        }
    </style>
</head>

<body>

    <!-- Main Content -->
    <div class="turtle-container">
         <!-- Wrapper for the "Edit Admin" form -->
        <div class="turtle-table-wrapper">
            <h2 style="text-align: center; color: #28ABE3;">Edit Admin</h2>
             <!-- Form for editing admin data -->
            <form action="/adminedit/<%= spec_admin.admin_id %>" method="POST">
                <!-- Table for input fields -->
                <table class="turtle-table">
                    <tbody>
                        <!-- Row for editing the username -->
                        <tr>
                            <th>Username</th>
                            <td>
                                <input type="text" class="form-control" id="username" name="username"
                                    value="<%= spec_admin.username %>" required>
                            </td>
                        </tr>
                        <!-- Row for editing the password -->
                        <tr>
                            <th>Password</th>
                            <td>
                                <input type="text" class="form-control" id="password" name="password"
                                    value="<%= spec_admin.password %>" required>
                            </td>
                        </tr>
                         <!-- Row for the "Save Changes" button -->
                        <tr>
                            <td colspan="2" style="text-align: center;">
                                <button type="submit" class="turtle-btn">Save Changes</button>
                            </td>
                            
                        </tr>
                    </tbody>
                </table>
            </form>
        </div>
    </div>

    <!-- Essential Scripts -->
    <script src="plugins/jquery/jquery.min.js"></script>
    <script src="plugins/bootstrap/bootstrap.min.js"></script>
</body>

</html>